Страница «Как формируется доверие в Rateus» — точки интеграции в UX
Цель

Сделать так, чтобы пользователь в любой момент мог понять,
как считается рейтинг и почему отзывы выглядят именно так.

Страница уже существует (или будет создана отдельно).
Задача — правильно встроить ссылки на неё в интерфейс.

1️⃣ Страница

URL:
/how-trust-works
(НЕ /for-specialists, НЕ /about, НЕ /faq)

Страница публичная, без авторизации.

2️⃣ Где и как добавить ссылки (ОБЯЗАТЕЛЬНО)
🔹 A. Карточка специалиста — рейтинг

Где:
В блоке рейтинга специалиста (там где звёзды и число отзывов)

Что добавить:
Иконку ⓘ рядом с рейтингом

Поведение:

клик по ⓘ → переход на /how-trust-works

без модалки, обычный переход

Текст tooltip при hover / tap:

Как формируется рейтинг

🔹 B. Статус рейтинга («Сформированный рейтинг»)

Где:
Текст Сформированный рейтинг под/рядом с рейтингом

Что сделать:

текст сделать кликабельным

или добавить ⓘ сразу после текста

Переход:
/how-trust-works#rating-status

(якорь на блок про сформированный рейтинг)

🔹 C. Экран «Все отзывы»

Где:
Вверху экрана «Все отзывы» (рядом с заголовком)

Добавить:
Текст-ссылку мелким шрифтом

Текст:

Как формируется доверие

Поведение:
Переход на /how-trust-works

🔹 D. Окно оставления отзыва

Где:
Под звёздами оценки (мелкий вторичный текст)

Текст ссылки:

Как работает рейтинг

Поведение:
Переход на /how-trust-works

🔹 E. Magic link (страница по ссылке из WhatsApp)

Где:
Внизу экрана, под кнопкой «Оставить отзыв»

Текст (мелкий, не акцентный):

Как формируется доверие в Rateus

Поведение:
Переход на /how-trust-works

3️⃣ ВАЖНЫЕ ОГРАНИЧЕНИЯ (обязательно соблюсти)

❌ Не добавлять ссылку:

в главное меню

в футер

в профиль пользователя

как отдельный пункт навигации

✅ Ссылка должна появляться только в контексте рейтинга и отзывов

4️⃣ Проверка готовности (чек-лист)

Считаем задачу выполненной, если:

ⓘ у рейтинга открывает страницу

«Сформированный рейтинг» кликабелен

в экране «Все отзывы» есть ссылка

в окне отзыва есть ссылка

в magic link есть ссылка

URL один и тот же: /how-trust-works

5️⃣ Ничего НЕ делать дополнительно

не переписывать тексты страницы

не добавлять маркетинг

не добавлять кнопки CTA

не добавлять регистрацию

Если хочешь следующим шагом —
могу сделать отдельное ТЗ на сам текст страницы 1:1,
чтоб